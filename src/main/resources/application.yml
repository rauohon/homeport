spring:
  profiles:
    active: dev
    include:
      - database

---
spring:
  profiles: prod
  devtools:
    livereload:
      enabled: true
server:
  port: 8080
  servlet:
    context-path: /

---
spring:
  profiles: dev
  devtools:
    livereload:
      enabled: true
server:
  port: 8881
  servlet:
    context-path: /
logging:
  level:
    root: debug

